import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.angular.usedfavorites',
  appName: 'angularflare',
  webDir: 'dist/angularflare'
};

export default config;
